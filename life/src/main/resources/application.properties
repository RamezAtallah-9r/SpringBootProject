spring.application.name=life
spring.mvc.view.prefix=/WEB-INF/
spring.mvc.view.suffix=.jsp
# ---------- SendGrid ----------
# ${VAR:default} = use the environment variable if set, otherwise the default.
# With "placeholder", EmailService prints the code in the console (demo mode).
sendgrid.api-key=${SENDGRID_API_KEY:placeholder}
sendgrid.from-email=${FROM_EMAIL:placeholder@test.com}